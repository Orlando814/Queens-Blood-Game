package sanguine.controller;

import static org.junit.Assert.assertTrue;

import org.junit.Test;

/**
 * This holds all the respective test for any class that implements the SanguineController
 * interface. This class's intent is to ensure correct behavior and code correctness.
 */
public class SanguineControllerTest {

  @Test
  public void testMock() {
    Appendable log = new StringBuilder();
    assertTrue(false);
  }
}
